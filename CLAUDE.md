# Claude Code Instructions

## Git & PR Workflow

- `gh` CLI is authenticated and works. Use it for PRs, merges, and GitHub operations.
- Create a branch, push, create the PR with `gh pr create`, and merge with `gh pr merge --merge --delete-branch`. Handle the full cycle; never leave manual steps for the user.
- Never push directly to main; always go through a PR branch.
- After merging, switch back to main and pull.
- Clean up: delete screenshot/test artifacts before committing. Add transient files to `.gitignore`.

## Supabase Deployment

- Supabase CLI (`supabase`) is installed and the project is linked to `bkyuxvschuwngtcdhsyg`.
- **Edge functions**: Deploy with `supabase functions deploy <function-name>`. Always deploy after modifying any edge function; do not leave this for the user.
- **Database migrations**: The local migration history is out of sync with remote. Do NOT use `supabase db push`. Instead, run SQL directly via the Supabase Management API:
  ```powershell
  $body = @{ query = "YOUR SQL HERE" } | ConvertTo-Json -Compress
  $headers = @{ 'apikey' = $env:SUPABASE_ACCESS_TOKEN; 'Authorization' = "Bearer $env:SUPABASE_ACCESS_TOKEN"; 'Content-Type' = 'application/json' }
  Invoke-RestMethod -Uri 'https://api.supabase.com/v1/projects/bkyuxvschuwngtcdhsyg/database/query' -Method POST -Headers $headers -Body $body
  ```
  The Supabase access token is in the user rules (starts with `sbp_`). Never commit it to source.
  Use `CREATE TABLE IF NOT EXISTS` and `CREATE INDEX IF NOT EXISTS` for idempotency. For RLS policies (no IF NOT EXISTS), query `pg_policies` first or accept that duplicates will error harmlessly.
- **Secrets**: Rotate via Supabase Dashboard. If a secret needs setting programmatically, use `supabase secrets set KEY=VALUE`.
- Always apply migrations and deploy functions yourself. Never tell the user to do it manually.

## Shell (PowerShell on Windows)

- No `&&` chaining; use `;` or separate commands.
- No heredocs (`<<'EOF'`); use simple `-m "message"` for git commits.
- No `tail`/`head` Unix commands; use `Select-Object -Last N` or read the file.
- `$$` in strings gets interpolated; avoid PL/pgSQL `DO $$ ... $$` blocks in inline SQL. Use separate policy creation statements instead.

## Project

- This is a **Vite + React 18 + TypeScript** app with **Supabase** backend
- Styled with **Tailwind CSS** and **shadcn/ui** components
- Animations via **Framer Motion**
- Edge functions live in `supabase/functions/` (Deno runtime)
- Router: React Router v6 with `createBrowserRouter` and lazy loading (`src/router.tsx`)
- Run `npm run build` to verify changes compile
- Node.js requirement: `>=22 <24`

## Architecture Quick Reference

- **Dashboard** (`/dashboard`) is the main hub - shows Memory Web (default) or Edge (`?view=edge`)
- Desktop: world-class desktop shell with `DesktopSidebar`, sticky top bar, optional right rail, and global Command Palette (Cmd/Ctrl+K). Authenticated routes wrapped by `AuthedLayoutRoute` / `CommandPaletteProvider`.
- Mobile: bottom nav (`BottomNav`) + full-screen views, floating voice FAB
- Authed routes (all wear the unified `DesktopShell`): `/dashboard`, `/memory`, `/context`, `/briefing`, `/decision`, `/goals`, `/enrich`, `/settings`, `/compliance`, `/profile`. Public: `/`, `/auth`, `/auth/callback`, `/build`
- Legacy routes (`/today`, `/voice`, `/pulse`, `/diagnostic`) redirect to `/dashboard`; `/think` redirects to `/dashboard?view=edge`
- AI: Vertex AI Gemini 2.0 Flash primary, OpenAI GPT-4o fallback, static tertiary
- **104 edge functions** in `supabase/functions/` (re-counted 2026-07-26; latest additions: `card-for-you`, `capture-lead`, `detect-trends`, `send-reactivation-nudge`)
- **77 custom hooks** in `src/hooks/` (re-counted 2026-07-26)
- **148 migrations** applied via Supabase Management API (re-counted 2026-07-26; latest: `20260706120000_news_trends.sql`)
- DB extensions in use: pgvector, pgcrypto, pg_cron
- Briefing v2 pipeline: lens → planner → fan-out (Perplexity/Tavily/Brave, 12s cap) → embed dedupe + score → curate → script (gpt-4o) → audio (ElevenLabs)
- Loading + Home/Briefing/Tune alignment (2026-06-28, LIVE): three fixes shipped together. (1) LOADING VALUE - the Home `GlobeLoader` no longer shows one static line; it rotates `src/components/system/loadingLines.ts` = evergreen AI-fluency one-liners + 3 durable trend lines + the leader's last-loaded real headlines (cached by `useCockpit` via `cacheHeadlines`), cross-faded (single static line under reduced-motion). (2) TUNE IS INSTANT - `useNewsPreferences` is now a MODULE-LEVEL shared store (`useSyncExternalStore`), so a save in `NewsPreferencesSheet` propagates to `useCockpit` immediately (before, each had its own `useState` copy, so tuning never moved the cards until remount). New `rankPersonalized` (`src/lib/newsPriority.ts`, unit-tested) re-ranks even an already server-personalized feed by lifting chosen lanes on the server's POSITION (never re-sorting by the generic `score`, which would undo personalization); neutral prefs are an exact identity. `useCockpit` uses `rankPersonalized` when `serverPersonalized`, `rankByPreferences` on the generic pool. (3) BRIEFING SHARES THE POOL - tune (boosted/bias) now rides the unified brain into the briefing: `brain-profile.ts` `toLensSource` carries `boosted`/`bias`, and the v2 query planner (`briefing-lens.ts` `planQueries`) leans toward those lanes + scan bias; and behind `BRIEFING_SOURCE_SHARED_POOL` the v2 pipeline merges today's `live_headlines_cache` candidates into the provider fan-out (`loadSharedPoolCandidates` → `dedupeAndScore`), so the spoken briefing covers the SAME stories as the Home cards (shared pool is the floor, leader-specific queries the ceiling; fallback ladder intact). FLAGS FLIPPED ON in Supabase secrets: `BRIEFING_V2_ENABLED_DEFAULT=true`, `BRIEFING_USE_BRAIN_PROFILE=true`, `BRIEFING_SOURCE_SHARED_POOL=true` (v2 keeps its v1 safety-net fallback). `generate-briefing` redeployed (v72). No new frontend env vars.
- Curation system (PRs #294-296, 2026-06-28, LIVE; CANONICAL DOC: `docs/CURATION-SYSTEM-SPEC.md` - read it before any Home-feed / Tune / briefing-curation work). The follow-ups that completed the tuning model: (#294) the Tune sheet `NewsPreferencesSheet` now APPLIES EVERY PICK LIVE through the shared store and dropped the below-the-fold Save button for a pinned "Done" dismiss. (#295) a chosen lane now DOMINATES the feed instead of a gentle lift: `newsPriority.ts` ranks a boosted lane in a `BOOST_BLOCK` above any score/position (uncapped, the variety cap was removed), the scan bias is a visible secondary sort (`practical` floats actionable lanes, `big` floats multi-source), and `useCockpit` is filter-forward (a chosen lane shows pure). (#296) GUARANTEED FLOOR OF 3 + ROLE FIT: `src/components/cockpit/laneReserve.ts` is a curated per-category evergreen reserve that tops a thin lane up to 3 ON-TOPIC cards (never off-topic filler); `src/lib/roleArchetype.ts` infers what the leader's JOB cares about across the 9 lanes from facts already held (role/title -> archetype, sector/industry -> light lift, no new questions/LLM) and `roleFitByCategory` feeds an optional `RoleFit` term into the ranker so a chosen lane is ordered by suitability to the person (a CFO's economics over product). Honest limit: on a genuinely thin news day the floor tops up with evergreen reserve cards (real headlines always lead); the lever for richer real supply is widening the per-lane gather in `live-headlines`. The three surfaces (Home cards / Tune / Briefing) are ONE system over ONE brain - see `docs/CURATION-SYSTEM-SPEC.md` for the full methodology + architecture, and `docs/CTRL-SYSTEM-SPEC.md` section 7 for the three main areas' purpose/objectives/outcomes.
- Home live-headlines pipeline (`live-headlines`, the Home "worth a look" feed): the quality lever is CROSS-VERIFICATION across free sources, not a fancier vendor. Gather in parallel from SIX sources (`_shared/news-sources.ts` `gatherAll`) - GDELT (open, no key, global breadth; flaky/429s so it has a 1.5s backoff retry, and the feed no longer depends on it) + Hacker News Algolia (open, no key, community-validated AI signal with points) + a curated reputable RSS allowlist (open, no key, quality controlled by `RSS_FEEDS`) + Brave News (`BRAVE_SEARCH_API`) + NewsAPI.org (`NEWSAPI_KEY`, mainstream aggregator breadth so more stories cross the corroboration bar) + Exa (`EXA_API_KEY`, neural/semantic search that surfaces the specific AI-native stories - agent frameworks, real deployments, ROI proof - keyword feeds miss, and does not rate-limit) - normalize to a common `RawArticle`, keep AI-native only (`_shared/news-ai-native.ts`), then CLUSTER near-duplicate headlines across sources (`_shared/news-cluster.ts`: title-token Jaccard, threshold 0.5). Each cluster's distinct-source count is the corroboration/trust signal; `worthSurfacing` admits 2+-source OR reputable (tier>=2) OR genuinely FRESH (<2d) single stories (so every category has depth for re-ranking); rank by `corroboration x reputation-tier x freshness x HN-engagement`, `selectBalanced` spreads the pick across the nine AI-native categories, and one batched grounded OpenAI pass (`_shared/news-synthesis.ts`, gpt-4o-mini, best-effort) writes a <=16-word, action-led "why it matters" line (falls back to the article snippet). Cards carry source + URL + publish age + a `corroboration` chip ("+2 sources"); rendered by `NewsHeadlineCard`. Shared, generic industry feed (not per-user), cached once/day in `live_headlines_cache` (one gather/day total, instant for users) and pre-warmed by the `live-headlines-prewarm` pg_cron job (10:30 UTC, `?force=1`). `?debug=1&force=1` returns per-source gather counts (no keys exposed) for ops. All news keys (Brave/NewsAPI/Exa/OpenAI) were already in Supabase secrets - no new keys provisioned. ENRICHMENT (not a standalone source): Artificial Analysis (`ARTIFICIALANALYSIS_API_KEY`) is loaded as a frontier-model ground-truth INDEX (`fetchAaModelIndex` -> cleaned name + intelligence index + blended $/1M + rank; ~540 models) and used to VALIDATE the news - `matchAaModel` spots a specific model named in a headline (version-token required so "GPT-5.5" matches but bare "Claude" doesn't) and attaches its real benchmark standing to that card as `benchmark` (rendered by `CockpitHero` as a quiet "Artificial Analysis · #N · index · $/1M" trust chip), plus a small score lift (a data-backed claim outranks an unbacked one). It is NEVER its own card - the earlier standalone/pinned AA card was removed per the "enrich, don't bolt on standalone features" principle. The frontend reads it via `useCockpit` (best-effort; falls back to the bundled `coldDeck.ts` so Home is never blank). Pure helpers unit-tested in `_shared/news-cluster.test.ts`. PERSONALIZATION (the feed is shared-but-reranked, NOT a per-user gather): the function returns a richer scored POOL (`POOL_SIZE` 20, each card carries `score` = the server importance and `sourceCount`), and the client re-ranks that pool into each leader's feed (`src/lib/newsPriority.ts`): a chosen lane now DOMINATES (uncapped `BOOST_BLOCK`), the scan bias is a visible secondary sort, role/business fit (`roleArchetype.ts`) orders within the lane, and a guaranteed floor of 3 tops a thin lane up on-topic (`laneReserve.ts`). See the canonical `docs/CURATION-SYSTEM-SPEC.md` for the full model (this clause supersedes the earlier "lift + per-category variety cap" description). Prefs live in the owner-scoped `news_preferences` table (`boosted_categories text[]`, `bias`), read/written by `useNewsPreferences` (localStorage-cached for instant first paint), and edited via `NewsPreferencesSheet` (the "Tune feed" picker on Home: 4 real-world priority groups + 3 bias options). Neutral prefs leave the server's world-importance order intact. Ranker unit-tested in `src/__tests__/newsPriority.test.ts`. Signal (own-decision) cards are deduped one-per-decision, labelled "Your decision" (never a news-category chip), and lead with the alert's `detail`.
- Skill Builder pipeline (`generate-skill-export`): voice/text transcript → boundedness check → Four Honest Tests triage (Test 4 = voice-lock / consistent creative output) → OpenAI JSON-mode extraction with a self-identified VOICE_PROFILE injected (no fabricated voice samples; reproduce the leader's real sample verbatim or describe the register, never invent a quote) → quality gate (now 16/16, incl. a required `## Learning loop` section) → agentskills.io-compliant ZIP that renders a structured 8-dimension `voice-profile.md`. **Now FREE for now (PR #204): the Edge Pro gate was REMOVED, so any authenticated user, including anonymous kit sessions, can build skills (the freemium-ladder WIP - `AutomatorTierBanner` / `useSkillBuildAccess` / `constants/skillTier.ts` / `_shared/skill-tier.ts` - was stripped).** The default `/context` flow is the **Automator** (PR #199, merge 24f7d15): `AutomatorFlow` runs brain-mined deliverable suggestions (`useSkillSuggestions` over `user_memory` blockers + decisions, role/sector fallback) → recognition pick-cascade → skill-ready with export, and `automatorModel.composeTranscript` maps the picks into the transcript fed to `generate-skill-export`. `SkillCaptureSheet` / `SkillPreviewSheet` are now dead code (unimported).
- Unified voice profile (PR #204): a single `ctrl_voice_profile` fact in `user_memory` (`fact_category` 'preference', `fact_subtype` 'communication_style'), captured by `VoiceStyleProfileSheet` (5 recognition picks OR a paste-extract power path via the `extract-voice-profile` edge fn), surfaced into generated skills by `_shared/memory-context-builder.ts`, and used by the harness. New files: `useVoiceProfile.ts`, `types/voiceProfile.ts`, `VoiceStyleProfileSheet.tsx`, `kit/KitVoiceProfileCard.tsx`, `lib/automatablePain.ts`. The Automator tone step is now voice-aware (a cold pick writes the profile via `toneToVoiceProfile`; a returning leader gets a "still sound like you?" confirmation; a paste-extract affordance opens the sheet in paste mode), and `automator/AutomatorScaffold.tsx` adds a desktop two-pane "your skill is taking shape" panel (desktop builder widened from a 402px phone column to max-w-4xl; mobile unchanged). Voice save enum bug fixed: `verification_status` 'confirmed' (invalid, silently 400-ing) -> 'verified'.
- Layered skill output (PR #204): the `mcp-context` MCP server gained `list_skills` + `get_skill` (a leader's own agent pulls their built CTRL skills live; read scope, Edge-Pro gated like the rest of that server), and `library/LibraryTab.tsx` gained a "Connect these to your agent" MCP banner + a per-item Download(.md). Three output destinations are now real: library (home) + MCP (live agent pull) + download.
- Decision Engine pipeline (`decision-engine`): POST a statement (source: advisor/capture/voice/fireflies) → decompose → verify (web-grounded claims) → cross-examine → advise, run in the background via `EdgeRuntime.waitUntil` while the frontend polls `decision_cases` + `decision_claims` per `stage` (mirrors the briefing streaming pattern). Model-capability/cost claims are additionally validated by Artificial Analysis as an evidence RETRIEVER (`retrievers.ts` `searchArtificialAnalysis` + `matchAaModel`): when a claim names a specific model, its real AA benchmark standing (intelligence index, blended $/1M, rank) is attached as primary-tier evidence and counts toward corroboration. The AA index is prewarmed in `pipeline.ts` and memoized in `retrievers.ts` (memo never caches a miss, 20s timeout) to avoid the verify-storm race; `decision_evidence.retriever` CHECK now includes `artificialanalysis` (migration `20260623133000`); evidence insert errors are now logged (were silently swallowed). `decision-watch` is an hourly pg_cron WATCH loop that re-verifies load-bearing claims and raises idempotent `decision_alerts` (surfaced in the Daily Briefing); `decision-eval` is the admin-only single-claim calibration harness. Tables (all RLS owner-scoped): `decision_cases`, `decision_claims`, `decision_evidence`, `decision_tensions`, `decision_alerts`, `decision_events`, `decision_eval_cases`. Hooks: `useDecisionEngine` (run + poll), `useDecisionInbox` (case list + open alerts).
- Brain engine (PRs #153-164, "limits" phases #187-189; migrations `20260615*_brain_*` + `20260616120000_memory_edges`): a fact-to-fact edge graph over Memory facts with Strengthen/Fix RPCs, evidence tiers, track-record depth, and reaction numbers. Honest residual gaps: the brain canvas Strengthen/Fix actions are UI-disabled (no backend RPC wired yet); brain edges are derived-not-stored; number-heroes fall back to words-led when current data is thin.
- Redesigned surfaces (PR #186, merge 1c01db5, 2026-06-16, prod-verified): the dark instrument palette, globally forced dark, and the emerald `ctrl.` wordmark, with a rebuilt mobile cockpit, decision spine, StoneRead reader, brain four-world rope canvas, capture flow, and onboarding.
- UX redesign (PRs #197-200, 2026-06-17, prod-verified): cockpit Home (behind `VITE_COCKPIT_ENABLED`) now leads with a time-aware greeting + a swipeable "worth a look" deck (`CockpitDeck`, news `briefings.segments` interleaved with own-signal `decision_alerts`, heart/skip via `useCockpit.recordDeckReaction`, dislikes persisted to the `feedback` table and down-weighted) + 3 value actions (briefing/decision/skill); the old "strongest signal" hero + AI-bets wall removed (#197/#200). `BrandLockup` (Mindmaker icon + `ctrl-logo.png`) replaces the generated `ctrl.` text in mobile + desktop headers/sidebars (#197/#200). Decision Map (`src/pages/DecisionMap.tsx`) is now ONE pinned-decision hero with a descriptive (never recommended) status + a connector rail; the long-press contest scroll-popup was killed for a quiet "Flag it" (#198). The Automator deliverable flow is now the `/context` default (#199, see Skill Builder bullet above).
- Kit program: RETIRED 2026-08-07 (PR #355). Six codes redeemed and six builds ever, one a test. It depended on externalities CTRL cannot observe (download, open another tool, paste, hope), so its success metric was unmeasurable by construction, and `list_skills`/`get_skill` over MCP already solved distribution. Its output was the deeper problem: `how-you-work.md` was a template string with interpolated bullet lists, so under `ctrl-build`'s own rule every line in it would have been rewritten `NOT ESTABLISHED:`. REDIRECTS WENT IN FIRST: `/kit` and `/kit/*` 301 to `/try` in `vercel.json`, so no link anyone was ever sent 404s, and `/download` navigates straight to `/try`. Deleted: the five routes, `src/pages/kit`, `src/components/kit`, the three `useKit*` hooks, `useIntakeFlow`, `lib/kit.ts`, `lib/kitPdf.ts`, `content/kits.ts`, the six edge functions (also deleted from the Supabase project), `_shared/kit-presets`, six kit scripts, the redeem e2e spec, the `kit-nudges-email` cron, the kit block in `config.toml`, the eight kit events in `track.ts` + `track-event`, and the Kit program from every price list. KEPT ON PURPOSE: the 87 `user_memory` rows carrying `source_type='kit'` (real leader memories; the enum value stays because Postgres cannot drop one and sweeping them would delete memories to tidy a schema), `capturePrimitives` + `_shared/email-shell` (both moved out ahead of the deletion; `/download` is live behind `VITE_FF_CAPTURE` and `send-decision-summary` is a production email), and every "kit" that means the skill ZIP (`BuildLap`, `free-skill-export`, `generate-skill-export`, `AutomatorCascade`) plus the `KitIntake` comments in `useSort` that document why the auto-advance ref pattern exists. STILL OUTSTANDING: the seven `kit_` tables exist but no code can reach them (34 artifacts / 6 builds / 5 codes / 4 journey events / 6 redemptions / 2 empty); they are NOT in the `delete-account` sweep, so their rows survive account deletion, which is a small privacy gap that dropping them closes.
- Main app polish (PRs #215-222, 2026-06-20/21, LIVE): the whole main CTRL app (not `/kit`) polished to one standard. Canonical spec: `docs/MAIN-APP-POLISH-SPEC.md` (read before any main-app work); approved news mock `prototypes/news-headline-cards.html`. North Star: CTRL is ONLY about building/orchestrating/productizing/getting-to-market the AI-native version of your business, never general business advice; general inputs are REFRAMED into the AI-native lens. The `decision-engine` gained a reframe Stage 0 + AI-native lock (#216, additive `decision_cases` cols reframed/reframed_statement/reframe_note/lifecycle_stage, original `statement` kept, banner surfaces the reframe). The news deck was rebuilt with 9 branded SVG category motifs (`CategoryMotif`, `src/types/newsCategory.ts` ids model/economics/tools/orchestration/product/governance/security/org/proof) + a rebuilt `NewsHeadlineCard`, and `generate-briefing` now AI-native-filters + category-tags every story (#215, `_shared/news-ai-native.ts`). The brain canvas squash was fixed: hardcoded landscape 760x520 viewBox letterboxed into portrait containers, now aspect-aware viewBox + fractional world positions + visible zoom controls (minScale 0.5) + rail-only-on-select (#217). Every main surface (home/compliance/settings/briefing/decision-page/automator) is now no-scroll on ALL devices + one ask per screen, with the presumptive/arrogant copy rewritten warm + first-timer-friendly + AI-native (#218-222). Briefing interests are now the 9 AI-native categories. Verified by component render + green CI + prod deploy READY + live prod landing serving new code; a live authed walk of the authed surfaces was not run.
- CTRL System / coherence campaign (PRs #228-232, 2026-06-21, LIVE, prod-deploy success): the app unified into ONE holistic instrument across the 4 levels. Canonical rule: `docs/CTRL-SYSTEM-SPEC.md` (read before any cross-surface work); approved home prototype `prototypes/home-chief-of-staff.html`. **The spine: CTRL is your AI-native chief of staff** (warm, advisory, first-person voice). **Home rebuilt** as the chief-of-staff "one adaptive thing" (#228): `CockpitView` is now ONE composed grid (header / hero / nav, never independently-laid-out blocks), `CockpitHome` shows ONE hero = top of the `useCockpit` ranked stream + a small SECONDARY peek (`deck[1]`) + an in-place "rest of what I'm tracking" reveal (`deck.slice(1)`) + a quiet briefing/weigh/build rail; the old `CockpitDeck` swipe deck was DELETED; heart/skip reactions preserved on the hero via `useCockpit.recordDeckReaction`. New components: `CockpitHero`, `CockpitStreamRow`, `cockpitGreeting.ts`. This FIXED the live home news-deck overlap (two independently-laid-out blocks fighting). **Voice** (#229): every tab entry point retitled to the chief-of-staff voice ("How I keep you safe", "Let's keep your story straight", "What you're aiming for", "Your daily read", "How your decision holds up", "I'll make you portable"); copy-only, legal/honesty text untouched. **Interaction** (#230 desktop, #232 mobile): `src/components/layout/PageTransition.tsx` (desktop, wraps `DesktopShell` `<main>`) + `MobilePageTransition.tsx` cross-fade ONLY the content keyed by pathname with chrome persistent; desktop sidebar active-nav glow (shared `layoutId`) now matches mobile `BottomNav`. **Compositional** (#232): `src/components/layout/MobileFrame.tsx` is the shared mobile shell (the `CockpitView` grid: persistent `AppHeader` + bounded no-scroll `<main>` + pinned `BottomNav`), applied to DecisionPage/Compliance/BriefingPage/ContextExport/Goals/TrackRecord/MemoryCenter/DecisionMap/EnrichPage/CockpitView; Dashboard `?view=edge` LEFT AS-IS (its load-bearing `data-edge-scroll` reveal/capture wiring; flagged not forced). **Visual** (#231): hardcoded hex moved onto `ctrl-ds` tokens (exact literals kept as CSS fallbacks so `CategoryMotif` renders byte-identical), one radius (`rounded-2xl` heroes) / padding / `hover:bg-secondary/50` scale. Verified by real-component + harness renders (auth-gated pages via a `/mf-verify` style harness) + green CI + prod-deploy success; a live authed walk was not run.
- CTRL 2028 radical-focus refactor (PRs #234-241, 2026-06-22, LIVE + live-verified on the real authed prod app, 8/8 QA gates pass): all 4 tabs + the shell rebuilt to "radical focus not overwhelm, native to each device". Canonical rule: `docs/CTRL-SYSTEM-SPEC.md` (now has section 6 the 2028 design language + the 6 principles + the session journey 1st/10th/100th + the LIVE-VERIFY rule); approved mocks `prototypes/{home,decisions,brain,you}-2028.html`. **PROCESS FIX (locked): verify on the REAL authed surface with real data (local Playwright login as QA acct `ctrl-qa-1782077550632@example.com`, autoconfirm ON; storageState in `_verify/auth/`), NOT a synthetic harness.** **Home** (#237): `src/components/cockpit/HomeFeed.tsx` + `DesktopHomeView.tsx` replace BOTH the mobile cockpit home AND the legacy desktop `DesktopMemoryDashboard` with ONE unified model = browsable headlines (mobile swipe-feed / desktop rail) + the 3 doors; the empty-Home bug is fixed via a bundled `coldDeck.ts` (generic AI-native headlines, never empty) + real own-signals woven in; `CockpitHome`/`CockpitStreamRow`/`CockpitDeck` were DELETED. New reusable loading primitive `src/components/system/SkeletonCard.tsx` (+ `SkeletonBar`/`LoadingCaption`). **Decisions** (#241): `src/components/operator/decision/Decision{Board,Capture,Orb,Running,ResultView}.tsx` - one calm board + ONE fast-capture input (mic embedded; killed the explainer wall + Record button + chips + floating FAB); the running state is a branded orb + a 4-step pipeline wired to the REAL `useDecisionEngine` stages. **Brain** (#240): `src/components/memory-web/BrainGraph.tsx` + `BrainCanvas.tsx` + `brainGraphModel.ts` (`computeViewBox`) - the graph is now CENTERED at every size (hub-anchored, half-extents per axis, aspect-corrected to the live canvas ratio every render via ResizeObserver, `preserveAspectRatio=xMidYMid meet`), fixing the upper-left clustering; `MemoryCenter` chrome calmed (one quiet stat line + a gentle capped "Verify N"). On MOBILE the tapped-node bond reader (`BondReader`) is now a bottom DRAWER (`@/components/ui/drawer`) that slides up OVER the canvas, so the brain graph keeps full size; the old inline `max-h-[44%]` panel under the canvas (which squashed the visual) was removed. Desktop still uses the slide-in right rail. **You** (#239): `src/components/track-record/*` - the 0/0/0 scoreboards are gone; cold = a promise, warm = an honest first pattern (never a deflating 0/N), rich = an earned calibration record, all from real data. **Shell** (#238): `src/components/system/BrandedAppLoader.tsx` replaces the raw spinner in `router.tsx` Suspense + `RequireAuth` + `InitializationLoader`; `resolveDisplayName` in `cockpitGreeting.ts` kills the ugly email-id greeting; `DesktopShell` desktop primary nav is now the 4 tabs (Home/Decisions/Brain/You) matching mobile, with the rest demoted to More/Account (no routes removed). NOTE: `BottomNav` is flag-gated - `cockpitNav` (4-tab) when `VITE_COCKPIT_ENABLED=true` (prod), `legacyNav` (6-tab) when off (so agent local-dev without the flag shows the legacy nav - a verify gotcha). Open MINOR non-blocker nits: desktop sidebar account footer still prints the raw email/id; mobile header wordmark renders as text vs the logo image; brain mobile hub sits a touch high; a transient pre-expired-session token-refresh console error (self-heals to 200).
- Unified onboarding -> decisions -> engagement loop (branch `claude/onboarding-decisions-engagement`, 2026-06-29): the three were patchwork (a legacy 40-min voice `OnboardingInterview` gate + a `VITE_COCKPIT_ENABLED` fork to legacy Memory dashboards, a loose onboarding->first-decision seam, and a cold-start TRAP where re-engagement never armed for a leader who never weighed a decision). Now ONE state-adaptive loop. **Spine:** `useCockpit` derives a lifecycle `userState` (new/dormant/active/power, off real timestamps + a 14d dormancy window) -> a `posture` ('guide' = guidance+kickstart for NEW/DORMANT/no-live-decision, 'partner' = decisiveness+evidence for active leaders), both on `CockpitData` (`src/types/cockpit.ts`). Posture + device context drive Home copy/lead (mobile = on-the-go quick read; desktop = deep work). **Onboarding** is now lightweight + INLINE (`src/components/cockpit/onboarding/InlineProfileSetup.tsx` + `useInlineProfile.ts`): captures industry+role direct to `user_memory` (the exact gate facts) + interests via the reused `SeedBeatsPrompt`, rendered in the Home feed zone for NEW leaders or the server profile gate (replaced `ProfileGateCard`'s navigate-away); `Dashboard.tsx` is now cockpit-only. **First-decision seam:** in the guide posture Home leads with a `KickstartCard` (`src/components/cockpit/KickstartCard.tsx`) - a real starter decision (`src/lib/starterDecisions.ts`, role-tailored) routed to `/decision` with prefill (`DeckCard.route`/`prefill`; `DecisionPage` also reads `?prefill=`). **Engagement:** `supabase/functions/send-reactivation-nudge` (daily pg_cron `reactivation-nudge` 13:00 UTC, verify_jwt=false) emails NEW (never-weighed) + DORMANT (lapsed >14d) leaders, de-duped on `leader_notification_prefs.reactivation_nudge_sent_at` (30d re-arm), batch-capped, lifecycle (not the daily_briefing opt-in). **Collapsed (no patchwork):** the `VITE_COCKPIT_ENABLED` fork + `legacyNav` + `Mobile/DesktopMemoryDashboard` + the voice `OnboardingInterview`/`DraftCockpit`/`onboarding/steps/*`/`useOnboardingInterview`/`useGuidedCapture`/`WelcomeTour` were DELETED (`EdgeOnboarding` kept - used by EdgeView). `BottomNav` is now the single 3-tab cockpit nav. Verified: typecheck + Vite build green, 225 unit tests pass, reactivation fn deployed + dry-run-verified live.
- Evidence-corpus sharpening (PR #321, 2026-07-03, LIVE; canonical doc: `docs/CTRL-SYSTEM-SPEC.md` section 9 - read it before any progression/memory-correction work). Grounded in the "AI-Native Operator" evidence corpus; four moves, all inside the existing shell. **(1) CORRECTION LOOP IS REAL**: `verify_memory_fact`/`fix_memory_fact` log `user_corrected`/`user_rejected`/`user_disputed` events to `memory_events` with the prior value (migration `20260703090000`, applied to prod); `extract-user-context` (v45) is correction-aware via `_shared/correction-guard.ts` (corrections ride the extraction prompt + a deterministic damping pass drops re-extractions of ruled-out values - also fixed the bug where rejected facts re-inserted because `is_current=false` left the dedup set); the verify swipe flow shows a "I noted what I got wrong" beat. Strengthen/Fix on the brain's `BondReader` are LIVE (were disabled buttons over complete RPCs), and `memory-edges-derive` finally has a caller (fired after each successful capture in `useMemoryWeb.submitInput`). **(2) DECISION MEMO**: every completed weigh copies as a board-ready one-page markdown memo (`src/components/operator/decision/decisionMemo.ts`, pure + tested; quiet Copy rows in `DecisionResultView` + `DecisionAnatomy`; no options section is fabricated - the engine stores no alternatives). **(3) CONTEXT FILE FIRST-CLASS**: `ContextFileButton` (pill on MemoryCenter's meta line, row on /context Step 1) one-clicks `my-ai-context.md` via `useMemoryExport` (was buried as the wizard's Raw Markdown option). **(4) CAPABILITY LADDER**: `src/lib/capabilityLadder.ts` (pure + tested) derives an EARNED 4-stage progression (orienting/operating/calibrating/compounding) from observed behaviour only (never XP/streaks); `useCapabilitySignals` (shared react-query, no new tracking); `CapabilityHeader` tops the You surface; Home's kickstart slot carries the ladder's next move when it lives outside the weigher (`applyNextMoveToKickstart`); `postureForStage` is the behaviour-identical seam for later posture adoption; `CockpitData.capabilityStage?` additive. Flag: `BRIEFING_INCLUDE_DECISION_ALERTS` (Supabase secret, default OFF) wires the previously-dormant `prependDecisionAlerts` into both briefing pipelines (`generate-briefing` v75 deployed). Note: lifecycle userState = engagement recency; capability stage = earned behaviour; they intentionally coexist.
- Settings audit / one-door dedup / design-system sweep (branch `claude/settings-audit-design-system-5oswzt`, 2026-07-04, LIVE): the Settings surface had drifted (an amateur design system, a duplicate briefing tuner, a broken track-record door, and cliché AI-speak). Fixed holistically against five principles: **one door**, **polished card language everywhere**, **plain copy**, **solid footers**, **interactions that work**. **(1) ONE DOOR for feed/briefing tuning**: `NewsPreferencesSheet` was split into a reusable `NewsPreferencesPanel` (the picker body) wrapped by the Home drawer shell AND rendered directly in Settings -> Interests (desktop Briefing tab + mobile section), so a tune in one place shows everywhere (they were two tables/UIs before - `news_preferences` lanes+bias vs a separate `BriefingInterestsTab` over `briefing_interests`). The people/companies + never-show watchlist (still `briefing_interests` via `useBriefingInterests`, still feeds the server briefing lens) is folded INTO that panel; the standalone `BriefingInterestsTab` was DELETED; the settings row is now "Tune your feed". **(2) ACTIVE-DECISION CONTROL-CENTRE**: the track-record "watch" cards (`AgedCallRow`) now read "Active decision" and carry Open / Strengthen / Archive, each wired to EXISTING doors via a shared `useDecisionActions` (open = `useDecisionEngine.load`+pin through a `PressureTestPanel` `openCaseId`/`strengthen` deep-link honoured by `DecisionPage`; strengthen = the existing `research('strengthen')`; archive = a `status='archived'` write mirroring `useResolveDecision`). No new features. **(3) TRACK-RECORD DOOR FIX**: the settings row now `closeSheet()` before `navigate('/track-record')` (the global drawer used to cover the page); the capability analysis (`CapabilityHeader`/`capabilityLadder`, one scorer - not duplicated) is now also surfaced in the Decisions->History embed. **(4) COPY CULL**: "Watching how it lands", "That is the point", "How they are turning out", "Still playing out", "keep an eye/keep watching" rewritten plain across track-record + decision surfaces (legal/honesty text untouched, no em dashes). **(5) DESIGN SWEEP**: new main-app primitives `src/components/system/surface.tsx` (`Surface`/`Eyebrow`/`SettingRow`/solid `SheetFooterBar`); `SettingsList` uses `SettingRow`; the transparent `NewsPreferencesSheet` "Done" footer is now a solid `SheetFooterBar`; off-token colours (`emerald-600`/`amber-600` dark-mode contrast bugs -> `text-accent`/`amber-500`), emoji (`PrivacyDataTab`) + `✓`/`-` glyphs (`EdgeProTab`) -> lucide, raw greys + a hardcoded `#00D9B6` button (`EditableField`) -> tokens, card radii unified to `rounded-2xl`; the dead Notifications row (led nowhere) removed. Verified: typecheck 0 new errors, 337 unit tests pass, build + prerender green, lint clean on touched files.
- Harness chain (PRs #336-345, 2026-08-04/05, LIVE; canonical docs: `CHALLENGE.md` at the repo root is the RULED register and is not relitigated, `docs/PHASE-0.5-HANDRUN.md` is the hand-run pack, `skills/ctrl-*` are the managed reference implementation and `skills/registry.md` their version record). Nine stages that turn a leader's graded judgement into a skill whose every rule points at the evidence it came from. Built because the old pipeline stripped provenance at render time: a situated remark ("we don't want a deck" about ONE engagement) became a standing rule ("Do not create decks for him"), and nothing in the schema could hold a citation or check one. **The spine** (migration `20260805090000`): `evidence_sources` + `evidence` (quotes located by exact OFFSET SLICE into a persisted source, never a fuzzy ratio - the spec's own 0.95/0.98 floors rejected its own founding quote; `situated` defaults true and promotion to standing is AUDITED via `standing_promoted_by`/`standing_evidence_ids`, written only by grade-sort, never by an LLM; third-party speech carries `retention_expires_at` + `redacted_at` so expiry EMPTIES the quote and keeps the row), `constructs`, `criteria` (person-scoped; no org layer, CH-07 cut it), `harness_runs` (pollable stage state, decision-engine pattern), `sort_items`/`sort_grades`, `ledger`, `proposals`, `skill_provenance`. All owner-RLS, all in the delete-account sweep. **1 INGEST** (`ingest-evidence`): extracts CANDIDATES not rules, max 12/source, every quote offset-verified, no contrast poles (the sort makes those), any INSERTED token rejects as fabrication while filler deletions pass. **2 SORT** (`build-sort`/`grade-sort`, `/sort`, `_shared/sort-composition.ts`): 5 constructs x 2 matched pairs (one pair each gave per-criterion n=2 and zeroed 3 of 10 constructs), 6 own + 4 peer + 3 repeat probes; WHOLE pairs held out (12, deliberately 2 more than the release floor of 10 so one skip cannot make Verified unreachable); PAIR-SPLIT RATE is the halt metric, replacing a leading manipulation question that asserted its own answer; open-form manipulation questions at 8/16/24; grades upsert. Reveal beats are honest: no kill before item 16 and only when `can_show_kill`, rubric reveal AFTER item 30 (showing it at 20 would anchor the third of held-out items still ungraded), self-agreement suppressed mid-flow (a live counter tells the grader some pieces repeat and destroys the measurement). `intended_dimension` cannot reach the render layer before its question is answered. **3a COMPILE** (`compile-standard`, `_shared/discrimination.ts`): criteria carry a deterministic PROBE (`contains`/`matches`/`max_words`/`within_first_paragraphs`/`has_number`/`has_proper_noun`/`not`); an unparseable observable is NEVER scored (`applyProbe` throws rather than returning false, because false is a scored answer to a question that cannot be asked); clustering runs on the criterion-APPLICATION vector, not grade vectors (which share no index set and make the statistic undefined); renders `my-standard.md` into `generated_artifacts` kind `standard` with AWAITING surviving for every section a sort cannot fill. **3b GENERATE**: router-plus-leaves package (SKILL.md capped 80 lines + `rubric/` + `exemplars/` + `references/` + `evals/`); every imperative carries `[C#]`/`[E#]` or is rewritten `NOT ESTABLISHED:`; a surface leaf declares its scope ONCE at the top (derived from the situations of the evidence its rules cite) and `core.md`/`general.md` stay unscoped so the james-harrabin guard still fires there; span-masking means no gate ever fires INSIDE verbatim evidence (CH-16, which would otherwise deadlock the package against its own exemplars); stage 7a DEMOTES any surviving unsourced rule deterministically (never deletes), because prov.everyRuleCited returned 6, then 1, then 4 across identical runs and an invariant held by asking a model is not an invariant. `baseline_unresolved_claims` reports the PRE-demotion count forever. **4 CRITIQUE** (`critique-artefact`, `/review`): 7a first, then three lenses with no mutual visibility (Standard on the person's criteria; Evidence and Signature house-level, advisory PERMANENTLY, labelled "CTRL's check"); Signature runs on a provider that is not the one RECORDED as generating the artefact (`llm-fallback.ts` gained ordered provider preference; the spec claimed this existed and it did not) and does not run at all rather than fake independence; a verdict whose quote is not a real substring drops to `insufficient evidence` and its quote is CLEARED; every `breaks` carries a revision; no aggregate score, because which criterion failed is the entire value. **The review surface is the ledger's only writer** (CH-01, found by three of four challenge lenses): mcp-context is read-only, so an installed skill tells CTRL nothing; Agree/Not-right on a verdict IS the row, and two `rejected` on one criterion is what Phase 8 promotes. **5 MEASURE** (`_shared/confusion.ts`, `scripts/eval-skill.mjs`): confusion matrix with WOULD_NOT_SEND as the positive class; precision, recall and TNR reported SEPARATELY, never one agreement number; a zero denominator returns null, never 0 (an absent measurement must be distinguishable from a measured zero); `releaseVerdict` is the only door to the word Verified and the frontend takes the WEAKER of stored and derived; held-out items are blocked from the exemplars at four layers because that leak inflates every number while looking clean. **6 DELIVER**: `get_skill` returns the WHOLE flattened package (a router whose leaves cannot be fetched resolves to nothing over MCP), `list_skill_files` + a `file` argument, and a stale pull gets one honest line naming both versions (never claims auto-update; "cannot be shown current" is a different sentence from "is current"); `mcp_pulls` logs one row per pull call and Library install state is derived ONLY from those rows, never from a build or download (no INSERT policy, so a client cannot manufacture evidence of its own use); `EdgeProCard` on the kit keep-it step. Plugin/marketplace/OAuth DEFERRED behind the standard-engagement measurement (CH-18). **8 LEARN** (`capture-week`, cron Sunday 20:00 UTC, `/proposals`): two strikes across five ISO weeks; Type A uncovered, then B false positive (auto-promoted on two pushbacks, the most urgent thing the system can find), then C drift which asks and never proposes a delta (three guards: the type cannot carry one, buildProposal re-checks, the table CHECKs it) and which requires the gate to have RUN in the window, because silence with no checks reports "you have not used the gate" as "your rules went stale". NO LLM in the pass: two strikes and the three types are arithmetic, so a model can never select a candidate. Every proposal carries `if_wrong` and a size delta; acceptance is never automated. **Honest open items**: the peer corpus ships EMPTY (writing prose and attributing it to a real person is a fabricated attribution; the deck runs 29 not 33 and reports the shortfall); `/sort`, `/review` and `/proposals` are URL-only and out of nav, so CH-01's falsification test (under ~1 check per active user per week the MCP write tools stop being optional) is live and measurable; the recall lever is the expressiveness of the probe grammar, NOT any `[U]` threshold, because most real criteria are not regex-checkable and die `untested`; Provisional is the normal outcome and Verified is rare by design. Live acceptance scripts, all passing against deployed functions: `smoke-ingest-evidence` (16), `smoke-sort` (20), `smoke-compile` (14), `smoke-critique` (12), `smoke-deliver` (13), `smoke-capture` (14), `smoke-short-sort` (21).
- Harness chain state and pick-up point: `docs/HARNESS-CHAIN-STATE.md` is the one page that says what is live, what was deferred and what trigger reopens it, what is still open and needs a human (peer corpus curation, the hand run, the URL-only surfaces), and the known limits. Read it before touching any of the nine stages.
- Sort depth + the chained compile (2026-08-07, LIVE, `build-sort` v3 / `grade-sort` v2 / `compile-standard` v3, all `verify_jwt=true` preserved). Two things were missing. **(1) THE SORT HAD ONE LENGTH.** `build-sort` now takes `depth: 'short' | 'full'` (short is the DEFAULT; anything unreadable resolves to short, because twenty minutes is a choice someone makes rather than something a malformed field does to them) and `SORT_BUDGET`/`SHORT_SORT_BUDGET` are threaded through `planDeck`/`chooseHoldOut` as a PARAMETER, so the full deck is byte-identical to what it was. The short budget is NOT a proportional halving and the arithmetic is the whole point: the hold-out counts are ABSOLUTE, so the full deck's `holdOut.pairs` of 4 applied to a six-pair deck makes `spreadIndices` take its `k >= n` branch and hold out FOUR OF SIX pairs, leaving two training pairs, which cannot reach `discriminationVerdict`'s 4-and-4 floor, so every criterion compiles `untested` and the sort produces NO standard while looking completely healthy (pinned by a test, not a comment). So the cut lands on the synthesised and peer halves and the OWN class is protected (5 own with 1 held out leaves the same 4 own items training as the full deck): 3 constructs, 6 pairs, 19 unique + 3 repeats, 4 held out. Every other floor clears (pair separation offset 6, 3 repeat probes so triage is measured, 6 pairs for the split denominator, kill gate at 16, screen-20 beat). One does not, ON PURPOSE: 4 held-out items is below `VERIFIED_MIN_HELD_OUT`, so the short deck can NEVER be Verified however well it is graded. That is a permanent ceiling, not a pending measurement, and the two read very differently to someone deciding whether to wait for a number, so `canReachVerified()` rides `stage_detail.can_reach_verified` and `depthCeilingNote()` says it in words on the completion screen. Every `_expected` in `stage_detail` now comes from the CHOSEN budget; left at the full deck's numbers `shortfallNotes` would tell a short-sort user their deck ran short of a target they never picked. **(2) STAGE 3a HAD NO DOOR.** `compile-standard` had no caller anywhere in the app, so no leader had ever had a `criteria` row, and with no criteria a built skill has nothing for its `[C#]` pointers to point AT. `useSort` now fires it when the last screen is graded (not behind a button: the panel had already promised it comes next), awaiting the in-flight grade promise first because grades are fire-and-forget and the last one can still be in the air when the deck ends. New `SortCompilePanel` reads the four endings honestly: `halted` (the pairs did not separate) and `template_and_voice` (the grader disagreed with themselves) are FINDINGS the instrument exists to produce, not errors, and their reason line comes from the backend verbatim so the run detail, the standard file and the screen cannot drift. The previously-dead `StandardCard` is the ready state. The label stays Draft, correctly: `compile-standard` passes `metrics: null` and never lifts its own label. **Measured head to head on live data**: the SUFFICIENCY counts are what the budget controls and they hold (9 accepted / 5 rejected per criterion, clear of the 4-and-4 floor); the KEEPER count tracks probe quality, not deck size (the full deck produced 0 keepers on two runs where the short deck produced 1), which is the already-documented probe-grammar recall limit and not a property of the short deck. `smoke-short-sort` therefore gates on sufficiency and only REPORTS keepers.
- Automator on the chain + the brain hero (PR #355, 2026-08-07, LIVE). **(1) THE AUTOMATOR WAS CITING CTRL'S OWN PROSE.** `generate-skill-export` splits the transcript into citable spans, and `AutomatorFlow` handed in its composed narration as that transcript. The narration is template prose from `automatorModel.ts` selected by tapping chips, so every surviving `[E#]` in a built skill resolved to a sentence WE wrote and attributed to the leader: a real, offset-verified provenance chain terminating in our own template library, which is worse than none because it looks sourced. Now split: `composeTranscript` -> `composeBuildBrief` (shapes the build, never stored or cited) and `composeOwnWords` (the leader's real material only - the seed mined from their own memory plus anything they typed) riding a new `own_words` body field that is the ONLY text offered for citation. Callers passing nothing keep the old behaviour (someone who typed the whole transcript owns all of it). `composeOwnWords` returns EMPTY for a leader who tapped through without typing, which is the honest answer: with no words of theirs and no compiled criteria there is nothing legitimate to cite, and `demoteUnpointedClaims` already rewrites those rules `NOT ESTABLISHED:`. A test pins that no chip fragment can reach the citable text. `persistCitedSpans` now stores the SAME string the spans were sliced from (passing the full transcript while spans came from `own_words` would store a source the offsets no longer index - the CH-13 corruption). Also populated `built_from` (the sort run, from `criteria.provenance`; null when criteria disagree, because a `built_from` naming one of several sources reads as a complete answer) and `baseline` (reuses `threeNumbers`); both existed on `BuildSkillZipInput` and neither had ever been set. `generate-skill-export` v50. **(2) THE BRAIN IS THE LANDING HERO.** `src/components/public/BrainDemo.tsx` renders the REAL `BrainGraph` (the same component `/memory` renders) on a static seed (`brainDemoSeed.ts`), which works because `BrainGraph` is pure and prop-driven with an `import type` MemoryEdge, so no auth/Supabase/hook code reaches the public bundle. The seed is a hand-built generic operator whose facts are deliberately dull and specific; nothing comes from a real leader's brain. The EDGES carry the argument, including a `tension` edge (wants out of delivery AND refuses to let anything ship unread), and `judgeCap` points at that, because a brain that only ever agrees with itself is not holding anything. Nodes assemble one at a time; whole on the first frame under reduced motion. **`/try` KEEPS THE WEIGH** - it is the right proof on the page where you then do one. Verified by rendering the BUILT site headless at 390 and 1280: all 7 nodes + hub draw, edges and legend render, no horizontal overflow, prerender still 3/3.
- Deploy gotcha, learned the hard way: `verify_jwt` is per-function and security-relevant. `mcp-context` authenticates with its own bearer token and the seven cron-invoked functions use a shared secret, so all need `verify_jwt=false`; a deploy that hardcodes `true` silently breaks them. Read and preserve the deployed value.
- All external API calls wrapped via `_shared/with-timeout.ts`. Structured logs via `_shared/logger.ts`. Stripe webhooks signature-verified + idempotent.

## Key Conventions

- No em dashes in any copy - use hyphens, semicolons, or parentheses
- PLAIN LANGUAGE, never app jargon (first-principles cleanup PRs #261-264): a leader must NEVER have to learn CTRL's own vocabulary. Renamed in UI copy - bond->connection, calibration->track record, deliverable->"something you do over and over", pressure test->weigh/check, Beats->Topics. Internal identifiers (GraphBond, CalibrationRead, onPressureTest, etc.) keep their names; only display strings change. Demystify genuine AI CONCEPTS a non-technical leader meets (orchestration, agents, inference) with the reusable `AiTermHint` (`src/components/system/AiTermHint.tsx`, popover; the news category `meaning` in `newsCategory.ts` is the single source of truth, surfaced on the card chip). Use AiTermHint for AI/industry terms ONLY, never for app vocabulary.
- FEWER DOORS (PRs #263-264): every entry screen leads with ONE primary action; extras sit behind a quiet disclosure or "More" (Automator Step 1, the search-first command palette, Settings collapsed 8 tabs -> 4 groups You/Briefing/Privacy & data/Account).
- Backend resilience is standardized (PRs #265-267): route every external call through `_shared/with-timeout.ts` (or an `AbortSignal.timeout`/outer `withTimeout`); the briefing OpenAI calls + v2 pipeline + the Artificial Analysis fetchers are all bounded now. Prefer the structured `_shared/logger.ts` (`createLogger(fn)`) over raw `console.*` for new code.
- Globally forced dark; ctrl-ds instrument palette; emerald #00D9B6 (--primary 171 100% 43%). App brand mark in headers/sidebars (mobile + desktop) is the `BrandLockup` (Mindmaker icon + `ctrl-logo.png` wordmark), which replaced the generated emerald `ctrl.` text (PRs #197/#200); not the old green Mindmaker logo.
- Mobile-first, no-scroll pattern on key pages
- Voice-first interaction where applicable
- Production URL: `ctrl.themindmaker.ai` (not leaders.themindmaker.ai)
